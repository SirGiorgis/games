Drop a PNG named `custom.png` here (or set `reference_image` on a character JSON).

Giorgis Fighting samples the photo for colors only. It never uploads the image or calls an AI API.
The game still plays if the file is missing.
